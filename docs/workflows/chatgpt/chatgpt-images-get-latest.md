# ChatGPT Images: Get Latest Result

- JSON: `workflows/chatgpt/chatgpt_images_get_latest_v1.json`
- Purpose: Open an existing image-generation chat by `chat_id`, inspect the latest assistant turn for image URLs, and report whether the requested set is ready.
- Required params: `chat_id`
- Optional params: `expected_image_count`
