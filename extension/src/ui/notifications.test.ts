import{beforeEach,describe,expect,it,vi}from'vitest';
let click:(id:string)=>void;const create=vi.fn(async(id:string)=>id);const tabsCreate=vi.fn();
beforeEach(()=>{create.mockClear();tabsCreate.mockClear();(globalThis as any).chrome={notifications:{create,onClicked:{addListener:(fn:any)=>click=fn}},tabs:{create:tabsCreate},runtime:{getURL:(x:string)=>`chrome-extension://id/${x}`}}});
describe('fleet notifications',()=>{it('notifies start and failure and deep links',async()=>{const m=await import('./notifications');m.installFleetNotificationClickHandler();await m.handleFleetRunNotice({job_id:'j',run_id:'r',workflow_id:'wf',phase:'started'});expect(create).toHaveBeenCalledWith('rzn-fleet:r',expect.objectContaining({iconUrl:'icons/brain-128.png',message:'wf started'}));await m.handleFleetRunNotice({job_id:'j2',workflow_id:'wf',phase:'failed',error_class:'timeout'});expect(create).toHaveBeenLastCalledWith('rzn-fleet:j2',expect.objectContaining({message:'wf failed (timeout)'}));click('rzn-fleet:r');expect(tabsCreate).toHaveBeenCalledWith({url:'chrome-extension://id/dashboard.html#runs/r'});});});
