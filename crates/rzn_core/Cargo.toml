[package]
name = "rzn_core"
version = "0.2.5"
edition = "2021"
license = "AGPL-3.0-only"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
schemars = "0.8"
jsonschema = "0.16"
tokio = { version = "1", features = ["net"] }

[build-dependencies]
serde_json = "1.0"
