# Card Resources

Structured connector catalogs and browser-side social profiles live here.

## Current Layout

- `social/`: card catalogs and browser connector profiles for social surfaces such as X.

The goal is simple: keep runtime data assets in `resources/`, not scattered across the repository root.
