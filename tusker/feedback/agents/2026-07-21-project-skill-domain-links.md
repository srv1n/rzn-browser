# Project skill domain links omit their directory

The repo project skill routes to `[[codebase/INDEX]]` and `[[workflow/CANON]]`, but the files live under `tusker/domains/<domain>/`. A literal routed read fails and requires an extra file search. Emit resolvable paths or document that domain links are relative to `tusker/domains/`.
